table:
  name: tags
  schema: data
array_relationships:
  - name: event_tags
    using:
      foreign_key_constraint_on:
        column: tag_word_id
        table:
          name: event_tags
          schema: data
select_permissions:
  - role: anonymous
    permission:
      columns:
        - tag_word
        - tag_id
      filter: {}
    comment: ""
  - role: user
    permission:
      columns:
        - tag_word
        - tag_id
      filter: {}
    comment: ""
